# AWS Lambda Requirements for GlucoLens Demo
# Optimized for cold starts and minimal package size

# Core FastAPI (minimal)
fastapi==0.125.0
pydantic==2.12.5
pydantic-settings==2.12.0

# Lambda integration
mangum==0.17.0

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9  # Fallback for some AWS Lambda environments

# Utilities
python-dateutil==2.8.2
