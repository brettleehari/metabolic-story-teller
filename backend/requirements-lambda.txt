# AWS Lambda Requirements for GlucoLens Demo
# Optimized for cold starts and minimal package size

# Core FastAPI (minimal)
fastapi==0.104.1
pydantic==2.5.0
pydantic-settings==2.1.0

# Lambda integration
mangum==0.17.0

# Database
sqlalchemy[asyncio]==2.0.45
asyncpg==0.31.0
psycopg2-binary==2.9.11  # Fallback for some AWS Lambda environments

# Utilities
python-dateutil==2.8.2
